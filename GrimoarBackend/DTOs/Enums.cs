namespace GrimoarBackend.DTOs
{

    public enum StranaEnum { SVETLO, TMA};
    public enum FrakceEnum { ND, DH, INKVIZICE, CIVIL};
    public enum SpecializaceEnum { SVETLY_MAG, OBRATEN, ZARIKAVAC, LECITEL, JASNOVIDEC, TEMNY_MAG, VLKODLAK, VEDMA, UPIR, VYKLADAC};
    public enum ZivelEnum { OHEN, VODA, ZEME, VZDUCH, MYSL, SMRT, NEZARAZENA};
    public enum StupenEnum { AAM, UM, TRIM, ARCH, MEGA};
    public enum StavKouzla { ZAZADANE, SCHVALENE };
    public enum StavReportuEnum { ZALOZENY, ROZPRACOVANY, ODESLANY, SCHVALENY};
    public enum KodChybyEnum { OBECNA_CHYBA, CHYBA_VSTUPU, NENALEZENO};

}
